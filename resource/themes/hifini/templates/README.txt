Put HTML overrides here mirroring resource/template/ paths.

Example:
  templates/pages/home.html  → replaces pages/home.html when this theme is active

Only files that exist here are overridden; others fall back to resource/template/.
